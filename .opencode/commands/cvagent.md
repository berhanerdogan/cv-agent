---
description: Generate a tailored CV from a job posting URL or text
---
Read AGENTS.md and profile.md fully.

The job posting is: $ARGUMENTS

If $ARGUMENTS is a URL, fetch the page using the browser skill. If it is plain text, parse it directly.

Then generate the CV following the instructions in AGENTS.md.